# Track Spec: Increase Interface Height

## Overview
Increase the height of the main application window by 40px to provide more vertical space for the UI.

## Requirements
- Update the initial window height in `main.go` from 200px to 240px.
- Update the dynamic window height in `App.tsx` from 480px to 520px.
